import express, {Request, Response} from "express";

const photo = express.Router();

photo.post('/add', (req: Request, res: Response) => {

})

photo.post('/remove', (req: Request, res: Response) => {

})

export default photo;